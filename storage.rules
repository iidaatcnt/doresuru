rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /stickers/{category}/{allPaths=**} {
      // 誰でも読み取り可能
      allow read: if true;
      // 開発中は誰でも書き込み可能
      allow write: if true; // TODO: 本番では管理者認証に変更
    }
  }
}
